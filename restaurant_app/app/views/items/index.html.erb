<h1>Item Index</h1>

<h6><%= link_to "Add a new item", new_item_path %></h6>
<h6><%= link_to "Home", profile_path %></h6>

<% @items.each do |item| %>

  <p><%= link_to( item.name, edit_item_path(item.id) ) %>, Cuisine: <%= item.cuisine %>, Price: $<%= item.price %></p>

<% end %>
