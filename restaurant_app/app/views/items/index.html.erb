<h6><%= link_to "Home", profile_path %></h6>

<h1>Item Index</h1>

<h6><%= link_to "Add a new item", new_item_path %></h6>
<p>(click item to edit)</p>
<% @items.each do |item| %>

  <p><%= link_to( item.name, edit_item_path(item.id) ) %>, Category: <%= item.category %>, Price: $<%= item.price %></p>

<% end %>
