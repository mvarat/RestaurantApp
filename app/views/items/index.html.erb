<h4>Menu</h4>

<h6><%= link_to "Add a new item", new_item_path %></h6>

<h5>Appetizers</h5>
<% @appetizers.each do |item| %>
     <p><%= link_to( item.name, edit_item_path(item.id) ) %>, $<%= item.price %></p>
<% end %>
<h5>Entrees</h5>
<% @entrees.each do |item| %>
     <p><%= link_to( item.name, edit_item_path(item.id) ) %>, $<%= item.price %></p>
<% end %>
<h5>Desserts</h5>
<% @desserts.each do |item| %>
     <p><%= link_to( item.name, edit_item_path(item.id) ) %>, $<%= item.price %></p>
<% end %>
<h5>Sides</h5>
<% @sides.each do |item| %>
     <p><%= link_to( item.name, edit_item_path(item.id) ) %>, $<%= item.price %></p>
<% end %>
<h5>Beverages</h5>
<% @beverages.each do |item| %>
     <p><%= link_to( item.name, edit_item_path(item.id) ) %>, $<%= item.price %></p>
<% end %>
